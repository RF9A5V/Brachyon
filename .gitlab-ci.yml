image: kisel/meteor-imagemagick-onbuild

before_script:
  - apt-get update -qq && apt-get install -y -qq imagemagick
  - meteor npm i

stages:
  - build
  - test
  - deploy

test-build:
  stage: build
  script: npm run test
